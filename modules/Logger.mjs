/*We need a logging tool that can automate most logging needs.
It should be globally available, but also support multiple instances.
It should be possible to use as a middleware, but also stand alone.
 */

import chalk from "chalk";
